import { MitTimePickerPipe } from './mit-time-picker.pipe';

describe('MitTimePickerPipe', () => {
  it('create an instance', () => {
    const pipe = new MitTimePickerPipe();
    expect(pipe).toBeTruthy();
  });
});
