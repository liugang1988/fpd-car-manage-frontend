/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IsEnhancePasswordPipe } from './is-enhance-password.pipe';

describe( 'IsEnhancePasswordPipe', () => {
  it( 'create an instance', () => {
    const pipe = new IsEnhancePasswordPipe();
    expect( pipe ).toBeTruthy();
  });
});
