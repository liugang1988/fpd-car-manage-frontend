import { SplitTimePipe } from './split-time.pipe';

describe('SplitTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitTimePipe();
    expect(pipe).toBeTruthy();
  });
});
